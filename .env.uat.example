# UAT Environment Configuration
# Copy this file to .env.uat for UAT deployments

# Environment Identifier (REQUIRED)
# Set to "uat" to enable Auth0 password protection
NEXT_PUBLIC_APP_ENV=uat

# ============================================================================
# Auth0 Configuration
# Get these values from your Auth0 dashboard: https://manage.auth0.com/
# ============================================================================

# Secret for encrypting session cookies (REQUIRED)
# Generate with: openssl rand -hex 32
AUTH0_SECRET='use [openssl rand -hex 32] to generate a 32 bytes value'

# Your application's base URL (REQUIRED)
# This is YOUR Next.js app's URL (where it's deployed), NOT Auth0's URL
# Used for callback redirects after authentication
# Examples:
#   - UAT: https://uat.yourapp.com
#   - Local testing: http://localhost:3000
AUTH0_BASE_URL='https://your-uat-domain.com'

# Auth0 issuer base URL (REQUIRED)
# This is YOUR Auth0 TENANT's URL (from Auth0 dashboard)
# Format: https://YOUR-TENANT.auth0.com or https://YOUR-TENANT.REGION.auth0.com
# Example: https://dev-abc123xyz.us.auth0.com
AUTH0_ISSUER_BASE_URL='https://YOUR_TENANT.auth0.com'

# Auth0 Application Client ID (REQUIRED)
# Found in Auth0 Dashboard → Applications → Your App → Settings → Client ID
AUTH0_CLIENT_ID='YOUR_AUTH0_CLIENT_ID'

# Auth0 Application Client Secret (REQUIRED)
# Found in Auth0 Dashboard → Applications → Your App → Settings → Client Secret
AUTH0_CLIENT_SECRET='YOUR_AUTH0_CLIENT_SECRET'

# Optional: Auth0 Audience (if using API authorization)
# AUTH0_AUDIENCE='https://your-api-identifier'

# Optional: Custom session configuration
# AUTH0_SESSION_ABSOLUTE_DURATION=604800
# AUTH0_SESSION_ROLLING_DURATION=86400
